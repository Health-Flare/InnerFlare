# Inner Flare 2.0.0 — release notes

Draft, human-written release notes for the next release, covering everything
that's landed since [v1.0.1](https://github.com/Health-Flare/InnerFlare/releases/tag/v1.0.1):
Quick Stats (merged to `main`) and the unlock screen rework
([PR #19](https://github.com/Health-Flare/InnerFlare/pull/19)).

This is a content draft, not a release trigger — it doesn't bump
`pubspec.yaml` or cut a tag. See "Cutting this release" at the bottom for
what's still needed to actually ship it.

---

## Store listing copy ("What's new")

Short version for Play Store / App Store, written to fit Play's 500-character
per-locale limit:

> **Inner Flare 2.0**
>
> A calmer way in: opening the app now shows one clear screen that explains
> what's about to happen before asking anything of you — no more surprise
> system prompts mid-launch. One tap is all it takes.
>
> Also new: Quick Stats on your dashboard. Two at-a-glance numbers — days
> since your last period, and an estimate to your next — right below your
> daily log, so you don't have to open Insights just to check in.

(365 characters — comfortably under the 500-char Play Store limit.)

---

## Full release notes

### A calmer way to unlock Inner Flare

Opening Inner Flare — for the first time, or any time after stepping away —
used to be able to jump straight to your device's raw "Enter Passcode"
prompt before the app had shown you anything of its own. That's fixed:

- A single screen now greets you first, every time, explaining plainly that
  your cycle data is encrypted on your device and needs Face ID, Touch ID,
  or your passcode to view. Nothing about your data is touched until you
  tap **Unlock**.
- First open and returning after being away now look and feel like the same
  moment, instead of two different mechanics.
- If an unlock attempt is cancelled or fails, you get a clear explanation
  and a single button to try again — never a repeat prompt you didn't ask for.

Nothing about *what's* protected has changed — your data was, and still is,
encrypted at rest and gated behind your device's own biometrics. This is
purely about *how it asks*.

### Quick Stats on your dashboard

Two configurable numbers now sit right below your daily log:

- **Days since your last period** — measured from either the start or the
  end of your last logged period, whichever matches how you think about it.
- **Estimated days to your next period** — a plain-statistics estimate
  based on your own logged history, with an honest "not enough data yet"
  when there isn't enough to go on.

Both are reconfigurable from the dashboard, so you're not stuck with
defaults that don't match how you track.

### Under the hood

A handful of routine dependency updates (Gradle, Android Gradle Plugin, the
Kotlin Android plugin, `desugar_jdk_libs`) — no user-visible change, kept
here for the technically curious and for F-Droid's changelog.

---

## Cutting this release

Not done as part of writing this file — listed here so it's not forgotten:

- [ ] Decide if this really warrants a major version bump (`2.0.0`) under
      this project's own convention, or should be `1.1.0` — these are two
      meaningful feature/UX changes, not a breaking change to anything
      external, so a minor bump may be more accurate. Pick one and update
      the version in this file's title before publishing.
- [ ] Bump `version:` in `pubspec.yaml` (both the version name and the
      build number after it).
- [ ] Merge [PR #19](https://github.com/Health-Flare/InnerFlare/pull/19) to
      `main` first — this release includes it.
- [ ] Tag `main` with `v2.0.0` (or whatever version is chosen above) —
      pushing that tag is what triggers the signed Android build/release
      and (once Apple secrets are in place) the iOS release, per
      `docs/deployment/android-release.md` / `docs/deployment/ios-release.md`.
- [ ] Paste the "Store listing copy" section above into the Play Console
      and App Store Connect "What's new" fields for this version.
- [ ] Consider overriding the GitHub Release body with the "Full release
      notes" section above — today `android-release.yml` auto-generates it
      from merged PR titles (`generate_release_notes: true`), which reads
      more like a commit log than something a user would want to read.
